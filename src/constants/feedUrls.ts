const baseUrl = `/.netlify/functions/server/`;

export const feedUrls = [
  `${baseUrl}kentcdodds`,
  `${baseUrl}freecodecamp`,
  `${baseUrl}hackernoon`,
  `${baseUrl}sekurak`,
  `${baseUrl}csstricks`,
  `${baseUrl}stackabuse`,
  `${baseUrl}alistapart`,
  `${baseUrl}akendi`,
];
